import "dart:io";

void main() {
  final N = int.parse(stdin.readLineSync() ?? "0");
  final ScoreList = stdin.readLineSync()!.split(' ').map(int.parse).toList();

  // final newList = [];
  // ScoreList.asMap().forEach((key, value) {
  //   newList.add(key, value)
  // })
 

  // print(NewList);
}

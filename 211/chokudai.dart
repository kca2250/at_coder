import 'dart:io';

// 動的計画法でする？
void main() {
  final S = stdin.readLineSync();
  final SLength = S.toString().length;
  final C = "chokudai";
  final CLength = C.length;

  print(SLength);
  print(CLength);

  // 途中退場
}

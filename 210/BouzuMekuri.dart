import 'dart:io';

void main() {
  final List = [];
  for (int i = 0; i < 2; i++) {
    final S = stdin.readLineSync();
    List.add(S);
  }
  final index = int.parse(List[0]);
  final nums = List[1];

}
